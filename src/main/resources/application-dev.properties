#数据库配置（凭据通过环境变量外部化，避免明文进入版本库；本地缺省值仅用于开发）
spring.datasource.druid.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.druid.url=jdbc:mysql://${DB_HOST:39.97.232.43}:${DB_PORT:3306}/${DB_NAME:groceryShop}?useSSL=false&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&allowMultiQueries=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
spring.datasource.druid.username=${DB_USERNAME:root}
#密码不再内置默认值，本地开发请通过环境变量 DB_PASSWORD 注入（原明文已视为泄露，应轮换）
spring.datasource.druid.password=${DB_PASSWORD}

#Druid 连接池参数
spring.datasource.druid.initial-size=5
spring.datasource.druid.min-idle=5
spring.datasource.druid.max-active=20
spring.datasource.druid.max-wait=60000
spring.datasource.druid.time-between-eviction-runs-millis=60000
spring.datasource.druid.min-evictable-idle-time-millis=300000
spring.datasource.druid.validation-query=SELECT 1 FROM DUAL
spring.datasource.druid.test-while-idle=true
spring.datasource.druid.test-on-borrow=false
spring.datasource.druid.test-on-return=false
spring.datasource.druid.pool-prepared-statements=true
spring.datasource.druid.max-pool-prepared-statement-per-connection-size=20

#Druid 监控页 (http://localhost:8090/druid)，口令通过环境变量外部化
spring.datasource.druid.stat-view-servlet.enabled=true
spring.datasource.druid.stat-view-servlet.url-pattern=/druid/*
spring.datasource.druid.stat-view-servlet.login-username=${DRUID_USERNAME:admin}
spring.datasource.druid.stat-view-servlet.login-password=${DRUID_PASSWORD:admin}
spring.datasource.druid.stat-view-servlet.reset-enable=false
#仅允许本机访问监控页（生产应配置内网网段白名单）
spring.datasource.druid.stat-view-servlet.allow=127.0.0.1

#Web 请求采集
spring.datasource.druid.web-stat-filter.enabled=true
spring.datasource.druid.web-stat-filter.url-pattern=/*
spring.datasource.druid.web-stat-filter.exclusions=*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*,/swagger-ui.html,/doc.html,/swagger-resources/**,/webjars/**

#SQL 性能与慢 SQL
spring.datasource.druid.filter.stat.enabled=true
spring.datasource.druid.filter.stat.log-slow-sql=true
spring.datasource.druid.filter.stat.slow-sql-millis=2000
spring.datasource.druid.filter.stat.merge-sql=true

#WallFilter 防 SQL 注入 (与 URL 上的 allowMultiQueries 保持一致)
spring.datasource.druid.filter.wall.enabled=true
spring.datasource.druid.filter.wall.config.multi-statement-allow=true
spring.datasource.druid.filter.wall.config.none-base-statement-allow=true

#启用过滤器
spring.datasource.druid.filters=stat,wall,slf4j

mybatis.mapper-locations=classpath:mapper/*.xml

# Feign
logging.level.com.grocery.groceryshop.feign=DEBUG
#出现错误时, 直接抛出异常
spring.mvc.throw-exception-if-no-handler-found=true
#不为工程中的资源文件建立映射
spring.web.resources.add-mappings=false
server.port=8090
